<Window x:Class="JetBrains.Etw.HostService.Notifier.DownloadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModel="clr-namespace:JetBrains.Etw.HostService.Notifier.ViewModel"
        mc:Ignorable="d"
        Visibility="Visible"
        WindowStartupLocation="CenterScreen"
        Icon="/EtwHostService.ico"
        Title="Downloading update"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        x:ClassModifier="internal">

  <Window.DataContext>
    <viewModel:DownloadingViewModel />
  </Window.DataContext>

  <Grid Margin="5">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <ProgressBar Grid.Row="0" Margin="5" Width="500" Height="25" Foreground="#FF009933" Minimum="0" Maximum="1" Value="{Binding Path=Progress,Mode=OneWay}" />
    <TextBlock Grid.Row="0" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Path=ProgressStr,Mode=OneWay}" />
    <Button Grid.Row="1" Margin="5" Width="60" HorizontalAlignment="Center" VerticalAlignment="Center" Content="_Cancel" Click="OnCancel" />
  </Grid>
</Window>